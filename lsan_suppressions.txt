# LeakSanitizer suppressions
# Known leaks in system libraries (not from project code).
#
# Empty since the native file dialogs left: the libdbus leaks they caused were the only
# entries. Kept as a file so the presets that point at it keep working, and so the next
# one has somewhere to go.
